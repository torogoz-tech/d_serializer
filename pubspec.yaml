name: d_serializer
description: "Static JSON serializer with annotation-based generation for Dart"
version: 1.1.2
repository: https://github.com/torogoz-tech/d_serializer
homepage: https://github.com/torogoz-tech/d_serializer
topics:
  - serialization
  - json
  - codegen

environment:
  sdk: ^3.11.5

dependencies:
  meta: ^1.12.0

dev_dependencies:
  build_runner: ^2.10.3
  d_serializer_builder: ^1.1.1
  test: ^1.24.0
